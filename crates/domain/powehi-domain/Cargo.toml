[package]
name = "powehi-domain"
description = "Pure domain: entities, value objects, events. Zero external deps."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
bytes = { workspace = true }
# SHA-256 for AbuseSubject::from_ip — pure computation, no I/O, keeps raw IPs
# (PII) out of storage, logs and the inter-region wire (prd.md §6.4).
sha2 = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
proptest = { workspace = true }

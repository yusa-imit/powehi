# cargo-deny configuration (deny.toml)
# Advisory ignores are in .cargo/audit.toml (cargo-audit config).

[advisories]
# cargo-deny advisory policy — see .cargo/audit.toml for cargo-audit ignores.
ignore = [
    { id = "RUSTSEC-2023-0071", reason = "rsa not compiled; sqlx-mysql transitive dep only" },
    { id = "RUSTSEC-2026-0124", reason = "DoS only; no fix in openmls_rust_crypto 0.5.1" },
]

[licenses]
# Acceptable SPDX license expressions for direct and transitive dependencies.
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "Unicode-DFS-2016",
    "CC0-1.0",
    "MPL-2.0",
    "OpenSSL",
    "Zlib",
]

[bans]
# Deny duplicate versions of security-critical crates to keep the dependency graph tight.
multiple-versions = "warn"
deny = []

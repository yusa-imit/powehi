{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main Powehi window",
  "windows": ["main"],
  "permissions": ["core:default"]
}

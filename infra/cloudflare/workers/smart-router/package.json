{
  "name": "powehi-smart-router",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Edge Worker smart routing for Powehi API (EU/AP geographic routing + PIPA guard)",
  "scripts": {
    "build": "wrangler build",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.1",
    "wrangler": "^4.20.0"
  }
}

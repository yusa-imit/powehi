replicaCount: 3

image:
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: "2"
    memory: "1Gi"
  requests:
    cpu: "200m"
    memory: "256Mi"

autoscaling:
  minReplicas: 3
  maxReplicas: 15
  targetCPUUtilizationPercentage: 60

region: eu-frankfurt
tier: "Tier1"

config:
  logLevel: "info"
  grpcTlsEnabled: true
  grpcPeers: ""
  # r2Endpoint/r2Bucket/vapidContact are intentionally unset here — this env
  # needs real Cloudflare R2 account values and a real VAPID contact URI
  # before it can serve real media or Web Push traffic. Do not fake these.

monitoring:
  serviceMonitor:
    enabled: true
    interval: "30s"
    additionalLabels:
      release: kube-prometheus-stack

otlp:
  endpoint: "http://otel-collector.monitoring.svc.cluster.local:4317"
  serviceName: "powehi-eu"

externalSecrets:
  refreshInterval: "1h"
  secretStoreRef:
    name: "powehi-secret-store"
    kind: "ClusterSecretStore"
  remoteRefs:
    databaseUrl: "powehi/prod-eu/database-url"
    redisUrl: "powehi/prod-eu/redis-url"
    r2AccessKeyId: "powehi/prod-eu/r2-access-key-id"
    r2SecretAccessKey: "powehi/prod-eu/r2-secret-access-key"
    vapidPrivateKey: "powehi/prod-eu/vapid-private-key"
    grpcTlsCert: "powehi/prod-eu/grpc-tls-cert"
    grpcTlsKey: "powehi/prod-eu/grpc-tls-key"
    grpcTlsCa: "powehi/prod-eu/grpc-tls-ca"
